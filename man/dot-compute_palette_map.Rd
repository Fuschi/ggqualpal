% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualpalr.R
\name{.compute_palette_map}
\alias{.compute_palette_map}
\title{Build a named palette map}
\usage{
.compute_palette_map(
  x,
  levels = NULL,
  drop = FALSE,
  na_color = "#BEBEBE",
  fixed = NULL,
  override = NULL,
  colorspace = list(h = c(0, 360), s = c(0.2, 0.5), l = c(0.6, 0.85)),
  cvd = c(protan = 0, deutan = 0, tritan = 0),
  bg = NULL,
  metric = c("ciede2000", "din99d", "cie76")
)
}
\arguments{
\item{x}{A vector treated as discrete.}

\item{levels}{Optional character vector specifying the complete set and order
of categories.}

\item{drop}{Logical; only used when \code{levels = NULL} and \code{x} is a factor.}

\item{na_color}{Colour associated with missing values when present in \code{x}.
Use \code{NULL} to avoid adding a dedicated \code{"NA"} entry.}

\item{fixed}{Named vector or named list of colours to fix for selected
categories. These colours are used during palette generation.}

\item{override}{Named vector or named list of colours to apply after palette
generation. These values replace the final colours of the specified
categories.}

\item{colorspace}{Passed to \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}.}

\item{cvd}{Passed to \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}.}

\item{bg}{Passed to \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}.}

\item{metric}{Distance metric passed to \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}. Must be one of
\code{"ciede2000"}, \code{"din99d"}, or \code{"cie76"}.}
}
\value{
A named character vector mapping each resolved level to a colour. If relevant,
the vector may also include an \code{"NA"} entry.
}
\description{
Build a named mapping from categories to colours.

The palette is built in three stages:
\itemize{
\item the final set of categories is determined from \code{x} and \code{levels};
\item colours in \code{fixed} are assigned first and used during palette generation;
\item colours in \code{override} are replaced at the end.
}

If \code{x} contains missing values and \code{na_color} is not \code{NULL}, the returned
mapping also includes a named \code{"NA"} entry.
}
\keyword{internal}
