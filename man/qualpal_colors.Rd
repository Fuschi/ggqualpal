% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualpalr.R
\name{qualpal_colors}
\alias{qualpal_colors}
\title{Map a vector of categories to qualpalr colours}
\usage{
qualpal_colors(
  x,
  levels = NULL,
  drop = FALSE,
  na_color = "#BEBEBE",
  fix = NULL,
  colorspace = list(h = c(0, 360), s = c(0.2, 0.5), l = c(0.6, 0.85)),
  cvd = c(protan = 0, deutan = 0, tritan = 0),
  bg = NULL,
  metric = c("ciede2000", "din99d", "cie76"),
  extend = NULL,
  direction = 1L
)
}
\arguments{
\item{x}{A vector (character, factor, numeric, etc.). Treated as discrete.}

\item{levels}{Optional character vector defining the full set \strong{and order}
of categories. If provided, it must contain all non-\code{NA} categories in \code{x}.
If \code{NULL}, factor levels (optionally dropped) or unique values are used.}

\item{drop}{Logical; only used when \code{levels = NULL} and \code{x} is a factor.
If \code{TRUE}, drop unused factor levels; if \code{FALSE} (default), keep them.}

\item{na_color}{Colour for \code{NA} values in \code{x} (default \code{"#BEBEBE"}). Use \code{NULL} to
leave \code{NA} unchanged.}

\item{fix}{Named hex colours to pre-assign to specific categories
(e.g. \code{c(A = "#CD0BBC", C = "#7acf79")}). Must be \strong{hex \verb{#RRGGBB}}
(case-insensitive). When \code{levels} is provided, names in \code{fix} must be a
subset of \code{levels}.}

\item{colorspace, cvd, bg, metric, extend, direction}{Passed to
\code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}. See its help for details.}
}
\value{
A character vector of hex colours (same length as \code{x}), with
\code{attr(result, "palette")} = named mapping level → colour (includes \code{"NA"}
if \code{na_color} is not \code{NULL} and \code{x} contains \code{NA}).
}
\description{
Returns a character vector of hex colours aligned to \code{x}, using a
qualitative palette generated via \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}.
If \code{levels} is supplied, it must include \strong{all} non-\code{NA} categories
found in \code{x}, otherwise an error is thrown. Fixed colours in \code{fix}
must also be a subset of \code{levels}.
}
\seealso{
\code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}
}
