% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualpalr.R
\name{map_qualpal}
\alias{map_qualpal}
\title{Map discrete categories to colours using qualpal}
\usage{
map_qualpal(
  x,
  levels = NULL,
  drop = FALSE,
  na_color = "#BEBEBE",
  fixed = NULL,
  override = NULL,
  colorspace = list(h = c(0, 360), s = c(0.2, 0.5), l = c(0.6, 0.85)),
  cvd = c(protan = 0, deutan = 0, tritan = 0),
  bg = NULL,
  metric = c("ciede2000", "din99d", "cie76")
)
}
\arguments{
\item{x}{A vector treated as discrete. It may be a character vector, factor,
or any vector coercible to character labels.}

\item{levels}{Optional character vector specifying the complete set and order
of categories to include in the palette. If provided, it defines both the
categories that will appear in the palette and their order. All non-missing
categories present in \code{x} must be included.}

\item{drop}{Logical; only used when \code{levels = NULL} and \code{x} is a factor.
If \code{TRUE}, unused factor levels are removed. If \code{FALSE}, all factor levels
are retained.}

\item{na_color}{Colour used for missing values. If \code{x} contains \code{NA} and
\code{na_color} is not \code{NULL}, the returned palette includes an additional
\code{"NA"} entry. Set to \code{NULL} to ignore missing values.}

\item{fixed}{Named vector or named list specifying colours to fix for
selected categories. Names must correspond to category labels. These
colours are included during palette generation so that the remaining
colours are chosen to be perceptually distinct from them by the optimisation algorithm used in qualpalr::qualpal().}

\item{override}{Named vector or named list specifying colours to assign
after palette generation. Names must correspond to category labels.
These colours replace the final values of the specified categories but do
not affect how the other colours are generated.}

\item{colorspace}{Passed to \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}. Defines the region of
colour space used to generate colours.}

\item{cvd}{Passed to \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}. Can be used to simulate colour
vision deficiencies.}

\item{bg}{Passed to \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}. Background colour used during
palette generation.}

\item{metric}{Distance metric used by \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}. One of
\code{"ciede2000"}, \code{"din99d"}, or \code{"cie76"}.}
}
\value{
A named character vector mapping categories to colours.
}
\description{
Generates a colour mapping for a discrete vector using
\code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}. The function returns a named character vector where
each category is associated with a colour.

Colours can be generated automatically, fixed for selected categories using
\code{fixed}, or replaced after generation using \code{override}.
}
\details{
The palette is constructed in three steps.

First, the set of categories is determined:
\itemize{
\item if \code{levels} is supplied, it defines the complete set and order of categories;
\item otherwise, if \code{x} is a factor, the factor levels are used;
\item otherwise, the unique non-missing values of \code{x} are used.
}

Second, colours are assigned:
\itemize{
\item categories specified in \code{fixed} keep the colours provided by the user;
\item colours for the remaining categories are generated automatically using
\code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}, taking the fixed colours into account.
}

Finally, if \code{override} is supplied, those colours replace the corresponding
entries in the final palette.

The order of the returned palette always follows the resolved category order.
}
\examples{
x <- c("A", "B", "C", "A")
map_qualpal(x)

x <- c("A", "B", "Other", "Smaller")
map_qualpal(
  x,
  fixed = c(
    Other = "transparent",
    Smaller = "#F5F5DC"
  )
)

x <- c("A", "B", "A", "C")
pal <- map_qualpal(x)
pal[as.character(x)]

# Final replacement after palette generation
map_qualpal(
  x,
  override = c(A = "#000000")
)

}
\seealso{
\code{\link[=pal_qualpal]{pal_qualpal()}}, \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}
}
