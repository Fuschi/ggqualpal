% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualpalr.R
\name{qualpal_palette}
\alias{qualpal_palette}
\title{Build a qualitative palette (level -> colour) via qualpalr}
\usage{
qualpal_palette(
  x,
  levels = NULL,
  drop = FALSE,
  na_color = "#BEBEBE",
  fix = NULL,
  colorspace = list(h = c(0, 360), s = c(0.2, 0.5), l = c(0.6, 0.85)),
  cvd = c(protan = 0, deutan = 0, tritan = 0),
  bg = NULL,
  metric = c("ciede2000", "din99d", "cie76"),
  extend = NULL,
  direction = 1L
)
}
\arguments{
\item{x}{A vector (character, factor, numeric, etc.). Treated as discrete
for the purpose of inferring categories if \code{levels = NULL}.}

\item{levels}{Optional character vector defining the full set \strong{and order}
of categories. If provided, it must contain all non-\code{NA} categories in \code{x}.
If \code{NULL}, factor levels (optionally dropped) or unique values are used.}

\item{drop}{Logical; only used when \code{levels = NULL} and \code{x} is a factor.
If \code{TRUE}, drop unused factor levels; if \code{FALSE} (default), keep them.}

\item{na_color}{Colour for \code{NA} values (default \code{"#BEBEBE"}). If \code{x}
contains \code{NA} and \code{na_color} is not \code{NULL}, the palette will include an
\code{"NA"} entry with this colour.}

\item{fix}{Named hex colours to pre-assign to specific categories
(e.g. \code{c(A = "#CD0BBC", C = "#7acf79")}). Colours are validated via
\code{grDevices::col2rgb()}. When \code{levels} is provided, names in \code{fix} must be
a subset of \code{levels}.}

\item{colorspace, cvd, bg, metric, extend, direction}{Passed to
\code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}. See its help for details.}
}
\value{
A named character vector of hex colours (level → colour). If \code{x}
contains \code{NA} and \code{na_color} is not \code{NULL}, the palette includes an \code{"NA"}
entry.
}
\description{
Constructs and returns a named vector \code{palette_map} using
\code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}, mirroring the behaviour of
\code{\link[=qualpal_colors]{qualpal_colors()}} but returning the palette instead of colours aligned
to \code{x}. If \code{levels} is supplied, it must include \strong{all} non-\code{NA}
categories found in \code{x}, otherwise an error is thrown. Fixed colours in
\code{fix} must also be a subset of \code{levels}.
}
\seealso{
\code{\link[=qualpal_colors]{qualpal_colors()}}, \code{\link[qualpalr:qualpal]{qualpalr::qualpal()}}
}
